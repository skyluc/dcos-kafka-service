ext {
    mesosVer = "0.28.0"
}

dependencies {
    compile "mesosphere:dcos-commons:0.8.1-SNAPSHOT"
    compile "org.apache.mesos:mesos:${mesosVer}"
}

idea.module {
    sourceDirs += file("$projectDir/src/generated/main/java")
}
